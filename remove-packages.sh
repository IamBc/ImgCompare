# Since this utility assumes you already have nodejs and npm 
# these 2 packages won't be removed. You should also delete recursively
# the dir you found this file in to remove all local modules, files and so on
sudo apt-get remove imagemagick
